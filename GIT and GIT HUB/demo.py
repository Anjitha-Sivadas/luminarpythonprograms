#git and github
# web based applications
# repository--folder or files
# clone - exact copy
# push - if completed on module,push that module repository
# pull--pass current update only
# if one module is completed, dat module will push to repository and from there it will pull
# to every developer to see chnages

# git clone==> remote repo>>>local storage
# git push==> local storage>>remote reposit
# git pull==> remote>>local storage (only updates)

# own system memory
# remte repo(using clone)>>local storage(git add)>>staging area(git commit)>>local repository(git push)>>remo repo

# local repo>>>remote repo

# git init---to initialize git
# git status---for listing untracked files (to list created files)
# git add-A---to move files to staging area
# git commit -m"my initial commit"--to move staging area--local repo
# git remote add hub https://github.com/Anjitha-Sivadas/luminardjango.git--to add remote repo
# git remote -v--to check whether it is added or not
# git push -u master--to push to remote repo